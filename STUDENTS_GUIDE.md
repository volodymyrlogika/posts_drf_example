# 📚 Інструкція для учнів: Розбір прикладу динамічного підвантаження постів

## 🎯 Мета
Розібратися як працює динамічне підвантаження постів з лайками за допомогою Django REST Framework і JavaScript.

## 🔍 Ключові файли для вивчення (в порядку важливості):

### 1. **Backend (Django + DRF)**

#### 📄 `posts/models.py` - Початок розуміння
```python
# Подивіться на модель Post:
# - Які поля в неї є
# - Як працює ManyToManyField для лайків  
# - Що робить @property likes_count
```

#### 📄 `posts/views.py` - Логіка API
```python
# Два головні view:
# 1. PostListAPIView - як повертаються пости з пагінацією
# 2. toggle_like - як працюють лайки через API
```

#### 📄 `posts/serializers.py` - Перетворення даних
```python
# Як Django об'єкти перетворюються в JSON для фронтенду
```

### 2. **Frontend (JavaScript)**

#### 📄 `templates/posts/posts_list.html` - Головний файл
```javascript
// Зверніть увагу на функції:
// 1. loadPosts() - як завантажуються пости
// 2. toggleLike() - як працюють лайки
// 3. Як створюється HTML через шаблонні рядки
```

### 3. **Налаштування**

#### 📄 `posts/urls.py` - Маршрути API
```python
# Два URL:
# - /api/posts/ для списку постів
# - /api/posts/{id}/toggle-like/ для лайків
```

## 💡 Алгоритм вивчення:

1. **Почніть з моделі** (`posts/models.py`) - зрозумійте структуру даних
2. **Перейдіть до API** (`posts/views.py`) - як дані віддаються фронтенду
3. **Вивчіть JavaScript** (`templates/posts/posts_list.html`) - як фронтенд отримує і показує дані
4. **Подивіться URL маршрути** (`posts/urls.py`) - як все з'єднується

## 🚀 Практичні завдання:

1. **Запустіть проект** і подивіться як працює в браузері
2. **Відкрийте Developer Tools** (F12) → Network → подивіться API запити
3. **Змініть код** - спробуйте додати нове поле або змінити дизайн
4. **Експериментуйте** з пагінацією - змініть кількість постів на сторінці

## 🔧 Команди для запуску:

```bash
# 1. Активуйте віртуальне середовище
.venv\Scripts\activate

# 2. Застосуйте міграції
python manage.py migrate

# 3. Створіть тестові пости
python add_posts.py

# 4. Запустіть сервер
python manage.py runserver

# 5. Відкрийте http://127.0.0.1:8000/
```

## ❓ Питання для самоперевірки:

1. Як працює пагінація в DRF?
2. Чому використовується ManyToManyField для лайків?
3. Як JavaScript отримує дані з Django API?
4. Навіщо потрібен CSRF токен при POST запитах?
5. Як додати нове поле в API відповідь?

---
**💡 Порада:** Не намагайтеся зрозуміти все відразу. Почніть з одного файлу, експериментуйте, змінюйте код і дивіться що відбувається!